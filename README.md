# MindfulBytes (Web)

Spring Boot + Thymeleaf web app.

## Requirements
- Java 17+

## Run
```bash
./mvnw spring-boot:run
```

Then open:
- http://localhost:8080/login

## Environment variables
- `GEMINI_API_KEY` (required for the chatbot endpoint `/api/chat`)
